# www → non-www (301 permanent) — must be first so it runs before any rewrites
https://www.prosperitylfs.com/* https://prosperitylfs.com/:splat 301

# Short branded link for the Life Insurance policy-review booking page —
# redirects straight to the existing Cal.com booking page, never through
# /book. Placed before the SPA fallback below so it takes priority.
/review https://cal.com/lorettastewart/life-insurance-consultation-prosperitylfs 301

# PDF and static asset bypass
/*.pdf 200
/assets/* 200

# All other routes → SPA fallback for client-side routing
/* /index.html 200
